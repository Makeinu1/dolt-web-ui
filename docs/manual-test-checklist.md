# Dolt Web UI — ブラウザ手動試験チェックリスト

このドキュメントはブラウザで実施する最終受け入れ試験のチェックリストです。
E2E スクリプト（APIレベル）が通過した後、UI の動作・表示・インタラクションを人手で確認します。

**前提条件:**
- `./dist/dolt-web-ui -config config.yaml` でサーバ起動済み
- Dolt SQL Server（ポート3306）が起動済み
- `Test` データベースに複数テーブルとデータが存在する状態
- ブラウザは Chrome または Firefox（最新版）
- セクション7（バージョン比較）の実施前に `merged/*` タグが **2つ以上**存在すること（Approve を2回以上実施しておく）
- セクション9（コンフリクト解決）の実施前に意図的なコンフリクトを作成しておくこと（手順は各セクション冒頭に記載）

**テスト実行順序の注意:**
セクション1→2→3→...の順に実施してください。各セクション開始時点でワークブランチが存在することを前提とします（ブランチ削除テストはセクション16に集約）。

---

## セクション 1: 起動・初期表示

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 1-01 | `http://localhost:8080` を開く | アプリが表示され、ヘッダー・グリッドが描画される | ☐ |
| 1-02 | ヘッダー左の `DB@Target` テキスト | 設定済みのターゲット・DB 名が表示される | ☐ |
| 1-03 | ブランチドロップダウン | `main` が初期選択されている | ☐ |
| 1-04 | テーブルセレクタ | テーブル一覧が表示される（`_cell_comments` は含まれない） | ☐ |
| 1-05 | テーブル選択後 | AG Grid にデータが表示される | ☐ |
| 1-06 | `main` ブランチ選択中 | ヘッダー右のアクションボタン（Commit / ↻ Refresh）が非表示 | ☐ |
| 1-07 | `main` ブランチ選択中 | セルをダブルクリックしても編集できない（read-only） | ☐ |
| 1-08 | `main` ブランチで行を右クリック | 「Show History」は表示される（Clone Row・Delete Row は非表示） | ☐ |

---

## セクション 2: ブランチ操作

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 2-01 | ブランチ DD → 「+ Create new branch」 | テキスト入力フィールドが表示される | ☐ |
| 2-02 | ブランチ名入力 → 作成 | 新しいブランチが作成され、自動切替される | ☐ |
| 2-03 | 作成したブランチでセル編集 | セルが編集可能になる | ☐ |

> ブランチ削除テストはセクション16（エッジケース）で実施します。

---

## セクション 3: データ編集・ドラフト可視化

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 3-01 | ワークブランチでセルをダブルクリック | セルが編集モードになる | ☐ |
| 3-02 | PK カラムをダブルクリック | 編集できない（カーソルが変化しない / 入力フィールドが出ない） | ☐ |
| 3-03 | 値を変更して Tab/Enter | 変更セルが **黄太字** で表示される | ☐ |
| 3-04 | テーブルセレクタ | 変更ありテーブルに `●` と件数 `(~1)` が表示される | ☐ |
| 3-05 | [⋮] メニューをドラフトあり状態で開く | 「Submit Request...」が disabled（グレーアウト）になっている | ☐ |
| 3-06 | 右クリック → Clone Row | 新しい行が緑ボーダー + 薄緑背景で追加され、PK が自動採番される | ☐ |
| 3-07 | 右クリック → Delete Row | 行に赤背景 + 取消線が付く | ☐ |
| 3-08 | 右クリック → Batch Clone... | バッチ生成モーダルが開く | ☐ |
| 3-09 | 右クリック → Show History | レコード変更タイムラインポップアップが表示され、変更セルが黄色ハイライトで old→new が確認できる | ☐ |
| 3-10 | [⋮] → Bulk Update | TSV 貼り付けプレビューが機能する | ☐ |

---

## セクション 4: コミット

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 4-01 | ドラフトあり → **Commit (N)** ボタン表示 | ヘッダー右に「Commit (N)」ボタンが表示される | ☐ |
| 4-02 | Commit (N) をクリック | コミットダイアログが開き、変更一覧が表示される | ☐ |
| 4-03 | ダイアログ内 × ボタンで個別キャンセル | その操作がドラフトから除外される | ☐ |
| 4-04 | コミットメッセージを**空**のまま確定 | エラーが表示され、コミットは実行されない | ☐ |
| 4-05 | コミットメッセージ入力 → 確定 | コミット完了後にドラフトがクリアされ、グリッドが更新される | ☐ |
| 4-06 | コミット後にテーブルセレクタ | `●` が消えている | ☐ |

---

## セクション 5: カラム表示制御

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 5-01 | `[⚙]` をクリック | カラム表示切替ポップアップが表示される | ☐ |
| 5-02 | カラムの非表示チェックを外す | グリッドからそのカラムが非表示になる | ☐ |
| 5-03 | ページリロード | 非表示設定が維持されている（localStorage 永続化） | ☐ |
| 5-04 | 別テーブルに切り替える | テーブル A の非表示設定がテーブル B に引き継がれない（独立した設定） | ☐ |

---

## セクション 6: Submit → Approve ワークフロー

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 6-01 | ドラフト空・コミット済み → [⋮] メニュー | 「Submit Request...」が有効（クリック可能）になっている | ☐ |
| 6-02 | Submit Request... → ダイアログ表示 | three-dot diff プレビューが表示される | ☐ |
| 6-03 | 概要入力 → 送信 | 申請完了メッセージが表示される | ☐ |
| 6-04 | `[📋 N]` バッジが表示される | ヘッダー右に承認待ち件数バッジが表示される | ☐ |
| 6-05 | `[📋]` をクリック | 承認者ビュー（RequestDialog）が開く | ☐ |
| 6-06 | テーブル選択 → diff プレビュー | DiffTableDetail に変更内容が表示される | ☐ |
| 6-07 | Approve ボタン | 承認完了後、バッジが消え、ブランチドロップダウンが次ラウンド（例: `wi/xxx/02`）に自動切替されている | ☐ |
| 6-08 | Reject ボタン | 申請が却下され、バッジが消える（ブランチは保持） | ☐ |

---

## セクション 7: バージョン比較（Compare Versions）

> **前提**: `merged/*` タグが2つ以上存在すること（事前に Approve を2回実施）

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 7-01 | [⋮] → Compare Versions... | HistoryTab が表示される | ☐ |
| 7-02 | バージョン（merged/* タグ）を2つ選択 → Compare | DB全体の変更サマリーが表示される | ☐ |
| 7-03 | テーブル行をクリック | AG Grid で行レベル diff が表示される | ☐ |
| 7-04 | 📥 ZIP ボタン | ZIP がダウンロードされ、`{table}_insert.csv` / `_update.csv` / `_delete.csv` が含まれている | ☐ |
| 7-05 | フルスクリーン | テーブル diff がフルスクリーンで表示される | ☐ |

---

## セクション 8: コミット履歴

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 8-01 | [⋮] → Commit Log | HistoryTab にコミット一覧が表示される | ☐ |
| 8-02 | コミット行をクリック | そのコミットの diff が表示される | ☐ |
| 8-03 | HistoryTab のフィルタ（デフォルト状態） | `[comment]` プレフィックスおよびauto-mergeコミットが非表示になっている | ☐ |
| 8-04 | 「コメントコミットを表示」チェックON | `[comment]` プレフィックスのコミットが一覧に表れる | ☐ |
| 8-05 | 「コメントコミットを表示」チェックOFF | `[comment]` コミットが再び非表示になる | ☐ |

---

## セクション 9: コンフリクト解決

> **前提（コンフリクト作成手順）:**
> 1. ワークブランチ A でセル X を値「AAA」に変更 → Commit → Submit → Approve（main に反映）
> 2. 別のワークブランチ B で同じセル X を値「BBB」に変更 → Commit
> 3. ブランチ B でブランチ DD → 「Sync」（または Submit 時の自動 Sync）を実行
> 4. 同一セルの競合でコンフリクトが発生する

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 9-01 | Sync 時にコンフリクト発生 | ConflictView オーバーレイが表示される | ☐ |
| 9-02 | テーブル選択 → 行ごとに Ours / Theirs 選択 | 選択が反映される | ☐ |
| 9-03 | 解決後に再コミット | コンフリクトが解消され、通常 Idle 状態に戻る | ☐ |

---

## セクション 10: セル単位コメント — 基本操作

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 10-01 | セル未選択時の 💬 ボタン | ボタンが disabled になっている | ☐ |
| 10-02 | ワークブランチでセルをクリック | ヘッダーの 💬 ボタンがアクティブになる | ☐ |
| 10-03 | セル選択 → 💬 クリック | 右サイドパネルが開き、コメント一覧（または空）が表示される | ☐ |
| 10-04 | テキストエリアにコメント入力 → 「追加」 | コメントがパネルに追加される | ☐ |
| 10-05 | Ctrl+Enter | コメントが追加される（キーボードショートカット） | ☐ |
| 10-06 | コメント追加後にセルのマーカー確認 | セル右上に amber の三角マーカーが表示される | ☐ |
| 10-07 | コメントパネルを**開いたまま**別のセルをクリック | パネルのタイトル（テーブル/PK/カラム）と内容が新しいセルのものに更新される | ☐ |
| 10-08 | 同じセルに2つ目のコメントを追加 | コメント一覧に2件表示される（作成時刻昇順） | ☐ |
| 10-09 | 🗑 ボタンでコメント削除 | コメントがパネルから消える | ☐ |
| 10-10 | 全コメント削除後のセルマーカー | 三角マーカーが消える | ☐ |
| 10-11 | ✕ でパネルを閉じる | パネルが閉じる | ☐ |

---

## セクション 11: セル単位コメント — ブランチ・表示制御

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 11-01 | main ブランチに切替 → セル選択 → 💬 | コメント一覧は表示されるが「追加」「削除」ボタンが非表示（閲覧のみ） | ☐ |
| 11-02 | テーブルを切り替える | 新しいテーブルのコメントマップが読み込まれる（マーカー更新） | ☐ |
| 11-03 | ブランチを切り替える | 新しいブランチのコメントマップが読み込まれる | ☐ |
| 11-04 | コメントありセルのマーカーとドラフト共存 | ドラフト編集中（黄太字）の同一セルに三角マーカーが重なって表示される | ☐ |
| 11-05 | ページ 2 → ページ 1 に戻る | ページ 1 のコメントマーカーが正しく表示される | ☐ |
| 11-06 | ページリロード（F5）後 | コメントは保存済み（即時コミット）、マーカーが再表示される | ☐ |

---

## セクション 12: コメント検索とジャンプ

> **フィルタの解除方法**: コメント検索ジャンプ後はグリッド上部のフィルタ入力欄をクリアすることでフィルタが解除されます。

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 12-01 | [⋮] → 🔍 コメント検索... | コメント検索モーダルが開く | ☐ |
| 12-02 | キーワードを入力 → 「検索」 | 結果テーブルが表示される（テーブル / PK / カラム / コメント / 日時） | ☐ |
| 12-03 | Enter キーで検索 | 検索が実行される | ☐ |
| 12-04 | ヒットなし | 「結果なし」メッセージが表示される | ☐ |
| 12-05 | 結果行をクリック | モーダルが閉じ、該当テーブルに切替 + 対象 PK の行のみ表示される | ☐ |
| 12-06 | ジャンプ後、グリッドのフィルタ入力をクリア | 全行が表示される（PK絞込が解除される） | ☐ |
| 12-07 | ジャンプ後に**別テーブル**に手動切替 | 別テーブルに PK フィルタが引き継がれない（または適切にリセットされている）こと | ☐ |
| 12-08 | Escape キー | モーダルが閉じる | ☐ |
| 12-09 | 日本語キーワード検索 | 正常にヒットする | ☐ |
| 12-10 | 複数テーブルにコメントがある場合の検索 | 全テーブルの結果が混在して表示される | ☐ |

---

## セクション 13: DiffCommentsPanel（レビュー・バージョン比較画面）

> **前提**: コメントが付いた変更のある Submit 申請が存在すること（コメント追加 → Commit → Submit の順で準備）

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 13-01 | 承認者ビューで変更テーブルを選択 | DiffTableDetail の下に変更セルのコメントが表示される | ☐ |
| 13-02 | コメントなしのテーブル | DiffCommentsPanel が表示されない（セクション非表示） | ☐ |
| 13-03 | バージョン比較画面でテーブルを選択 | 同様に DiffCommentsPanel が表示される | ☐ |
| 13-04 | DiffCommentsPanel の表示内容 | pk, カラム名, 日時, コメント文が表示される（読み取り専用、削除ボタンなし） | ☐ |

---

## セクション 14: コメントとバージョン管理の連携

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 14-01 | コメント追加直後にデータ変更 → **Commit** ボタンをクリック | STALE_HEAD エラーが発生しない（refreshHead 済み） | ☐ |
| 14-02 | コメント追加後に ↻ Refresh ボタンの表示確認 | （コメント追加で HEAD が変わるため）ヘッダーに ↻ Refresh が一瞬現れ、自動解消されるか確認 | ☐ |
| 14-03 | ワークブランチでコメント追加 → Submit → Approve | main ブランチに切替 → 該当テーブルのセルを選択 → 💬 でコメントが存在することを確認 | ☐ |
| 14-04 | コメントありの行を Delete Row → Commit | 💬 を開くとコメントが削除されている（空一覧） | ☐ |
| 14-05 | コミット履歴にコメントコミットが記録される | 「コメントコミットを表示」ON で `[comment] table/pk/col` コミットが確認できる | ☐ |

---

## セクション 15: ページング・パフォーマンス

> **前提**: ページ数が複数になる量（page_size=50 以上）のデータが存在すること

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 15-01 | 複数ページあるテーブルでページネーション表示 | `◀ 1/N ▶` のページネーションが表示される | ☐ |
| 15-02 | ページ切替 | 次ページのデータが表示される | ☐ |
| 15-03 | テーブル切替時の表示速度 | 明らかなフリーズなくデータが表示される | ☐ |
| 15-04 | ページ切替後のコメントマーカー | 別ページに移動してもそのページのコメントマーカーが正しく表示される | ☐ |

---

## セクション 16: エッジケース・エラーハンドリング

| # | 確認内容 | 期待結果 | 結果 |
|---|---------|---------|------|
| 16-01 | 空のコメントで「追加」 | エラー表示。コメントは送信されない | ☐ |
| 16-02 | 5001文字のコメントテキスト | エラー表示。コメントは送信されない | ☐ |
| 16-03 | main ブランチで 💬 → 「追加」ボタンが存在しないことを確認 | UIレベルで追加ボタンが非表示（MainGuard） | ☐ |
| 16-04 | ブランチ削除 → main に自動切替 | ブランチ DD → 🗑 削除 → 確認後にブランチが削除され、main に切り替わる | ☐ |
| 16-05 | ブランチ削除直後の状態 | 削除したブランチがドロップダウンから消えている | ☐ |
| 16-06 | CLIRunbook | CLI エラー状態（SchemaConflict 等）時に最小化可能なガイドが表示される | ☐ |

---

## 試験結果サマリー

| セクション | チェック数 | 合格 | 不合格 | 備考 |
|-----------|-----------|------|--------|------|
| 1: 起動・初期表示 | 8 | | | |
| 2: ブランチ操作 | 3 | | | |
| 3: データ編集 | 10 | | | |
| 4: コミット | 6 | | | |
| 5: カラム表示制御 | 4 | | | |
| 6: Submit → Approve | 8 | | | |
| 7: バージョン比較 | 5 | | | |
| 8: コミット履歴 | 5 | | | |
| 9: コンフリクト解決 | 3 | | | |
| 10: コメント基本操作 | 11 | | | |
| 11: コメント表示制御 | 6 | | | |
| 12: コメント検索・ジャンプ | 10 | | | |
| 13: DiffCommentsPanel | 4 | | | |
| 14: コメント×バージョン管理 | 5 | | | |
| 15: ページング | 4 | | | |
| 16: エッジケース | 6 | | | |
| **合計** | **108** | | | |

---

試験日: ___________
試験者: ___________
バイナリバージョン（コミットハッシュ）: ___________
